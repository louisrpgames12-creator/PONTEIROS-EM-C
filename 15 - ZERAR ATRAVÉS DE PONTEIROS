#include <stdio.h>
void zerar_contador(int *inteiro) {
    *inteiro = 0;

}

int main() {
    int inteiro;

    printf("ME INFORME UM NUMERO: ");
    scanf("%d", &inteiro);

    zerar_contador(&inteiro);

    printf("ZEROU A VARIAVEL INTEIRO = %d", inteiro);
    

    return 0;
}
